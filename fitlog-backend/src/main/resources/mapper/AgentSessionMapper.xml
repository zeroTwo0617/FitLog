<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.fitlog.backend.repository.AgentSessionMapper">
    <sql id="columns">
        id, openid, session_id, messages, updated_at
    </sql>

    <select id="selectByUserAndSession" resultType="AgentSession">
        SELECT <include refid="columns"/> FROM agent_sessions WHERE openid = #{openid} AND session_id = #{sessionId}
    </select>

    <insert id="insert" parameterType="AgentSession" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO agent_sessions (openid, session_id, messages)
        VALUES (#{openid}, #{sessionId}, #{messages})
    </insert>

    <update id="updateMessages">
        UPDATE agent_sessions SET messages = #{messages}, updated_at = CURRENT_TIMESTAMP WHERE id = #{id}
    </update>
</mapper>
