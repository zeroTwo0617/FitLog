# FitLog 开发文档

> 当前版本：数据与界面统一版
> 更新时间：2026-08-03
> 项目形态：微信小程序原生 + 微信云开发 CloudBase

这份文档只记录当前有效的架构和开发约定。历史方案和旧提交说明不再堆在这里，改动历史以 Git 为准。

## 1. 产品边界

FitLog 是一个个人训练记录工具，核心闭环是：

1. 选择动作并记录每组次数、重量、休息。
2. 保存后形成一条训练会话。
3. 日历按训练会话展示训练日，并查看当天训练和饮食。
4. 历史按训练会话查看详情。
5. 统计基于训练会话和组明细计算训练量、最大重量和估算 1RM。
6. 教练、饮食、计划和身体数据围绕同一账号数据工作。

当前不引入独立后端。CloudBase 是唯一远端数据源，客户端只做页面查询和轻量聚合。

## 2. 技术架构

- UI：微信小程序原生 WXML / WXSS / JavaScript
- 数据：CloudBase 文档数据库
- 身份：wx.cloud.init({ traceUser: true }) 自动注入 _openid
- 权限：集合设置为“仅创建者可读写”
- 纯逻辑：放在 utils/*Data.js，不依赖 wx
- 页面：负责查询、状态管理和渲染
- 测试：Node 运行纯函数和页面 mock 测试

### 目录职责

| 目录 | 职责 |
| --- | --- |
| app.js | 云环境初始化、全局主题和页面壳 |
| app.wxss | 全局设计 token、按钮、卡片、表单和通用状态 |
| utils/config.js | 云环境 ID 和集合名唯一配置 |
| utils/cloud.js | 惰性获取数据库实例 |
| utils/workoutData.js | 训练日期和训练会话摘要的唯一口径 |
| utils/statsData.js | 训练量、最大重量、日历数据、1RM 和身体趋势 |
| utils/historyData.js | 历史排序和训练详情分组 |
| utils/nutrition.js | 饮食日期聚合和宏量营养换算 |
| utils/planData.js | 计划与记录页 session 的转换 |
| pages/ | 页面查询、交互和展示 |
| cloudfunctions/ | 服务端权限或批处理云函数 |
| scripts/ | 纯逻辑和数据生成测试 |

### 2.1 视觉与尺寸基准

参考稿以 375 × 812 px 手机画布为基准，小程序用 rpx 近似映射，页面只使用以下共享尺度：

| 项目 | 参考尺寸 | 小程序基准 |
| --- | ---: | ---: |
| 页面左右内边距 | 18 px | 36 rpx |
| 页面主标题 | 27 px | 54 rpx |
| 区块标题 | 14 px | 28 rpx |
| 正文 | 13 px | 26 rpx |
| 辅助文字 | 9–11 px | 18–22 rpx |
| 底部导航文字 | 9 px | 18 rpx |
| 记录输入格 / 主按钮 | 40–50 px | 80–100 rpx |

视觉 token 统一维护在 `app.wxss`：深色背景 `#0A0C0B`、一级卡片 `#151915`、嵌层 `#1C221D`、边线 `#272F29`、主色 `#C6F24E`、提示橙 `#FF7A45`。页面 WXSS 不应重新定义同名颜色或使用负字间距；标题、正文、辅助信息优先复用全局类和 token。

底部导航是实体深色浮层，不使用透明白底。当前页面同时通过图标、文字和 Volt 圆点标识，组件在页面重新显示时根据当前 route 自动同步选中项。

## 3. 数据模型

### 3.1 通用约定

- 每条用户数据由 CloudBase 自动写入 _openid。
- 页面不得使用另一个用户 ID 过滤数据。
- 日期展示统一使用 YYYY-MM-DD。
- dateStr 是用户本地训练日的权威字段。
- date / createdAt 是时间戳，用于排序和审计，不替代 dateStr。
- 集合名只能从 utils/config.js 的 COLLECTIONS 读取。

### 3.2 workouts：训练会话事实表

一条文档代表一次训练：

    {
      _id: 'session-id',
      _openid: 'owner-openid',
      date: Date,
      dateStr: '2026-08-02',
      title: '练背日',
      planId: '',
      exercises: [
        { exerciseId: 'deadlift', name: '传统硬拉', setCount: 3 }
      ],
      setTotal: 3,
      createdAt: Date
    }

职责：

- 决定训练次数。
- 决定哪一天算训练日。
- 提供日历和历史列表的会话摘要。
- 通过 _id 与 sets.sessionId 关联。

### 3.3 sets：训练组明细表

一条文档代表一次训练中的一组：

    {
      _id: 'set-id',
      _openid: 'owner-openid',
      sessionId: 'session-id',
      exerciseId: 'deadlift',
      exerciseName: '传统硬拉',
      setIndex: 1,
      reps: 5,
      weight: 100,
      restSec: 120,
      completed: true,
      createdAt: Date
    }

职责：

- 计算训练容量：reps * weight。
- 计算动作最大重量。
- 计算 Epley 估算 1RM。
- completed === false 的组不计入指标。
- 缺少 completed 的旧数据按已完成处理。
- 没有对应 workouts._id 的孤立组不计入统计。

### 3.4 其他集合

| 集合 | 关键字段 | 用途 |
| --- | --- | --- |
| nutritionLogs | dateStr、mealType、protein、carbs、fat、calories | 每日饮食 |
| dietPlans | dailyTarget.calories | 饮食目标 |
| bodyMetrics | dateStr、weight、fatPct、height | 身体数据 |
| plans | name、items[] | 训练计划 |
| users | _openid、nickName、goal、level | 用户档案 |
| agentSessions | 会话消息和模式 | 教练会话 |

## 4. 唯一数据口径

### 4.1 日期口径

所有页面调用 utils/workoutData.dateKey(record)：

1. 优先读取合法的 record.dateStr。
2. 没有 dateStr 时兼容 date。
3. 仍没有时兼容 createdAt。
4. 无法解析的记录不进入日期统计。

禁止页面直接写 item.dateStr 来推导训练日。

### 4.2 训练日口径

训练日只由 workouts 决定。

因此：

- 用户保存了一次训练，即使所有组都没有勾选完成，该日期仍会出现在日历和统计的训练日计数。
- 该训练的容量和 1RM 可以为 0，因为这些指标需要有效的已完成组。
- 日历、首页打卡、统计本月训练日和历史训练日数必须使用 workoutData.summarize(workouts)。

### 4.3 指标口径

指标由 workouts + sets 联合计算：

1. 用 workouts._id 映射 sets.sessionId。
2. 过滤 completed === false。
3. 过滤无效的次数、重量和动作。
4. 再计算容量、最大重量和 1RM。
5. 不允许用 workout 摘要里的 setTotal 计算容量。

## 5. 页面职责

| 页面 | 负责 | 不负责 |
| --- | --- | --- |
| 首页 | 今日状态、快捷入口 | 自己重新聚合训练日期 |
| 记录 | 编辑当前训练并写入 workouts、sets | 统计历史指标 |
| 日历 | 月历、某天训练详情、当天饮食 | 计算训练容量 |
| 历史 | 会话列表和详情 | 推导新的统计口径 |
| 统计 | 训练次数、训练量、最大重量、1RM | 重复展示月历详情 |
| 饮食 | 某天饮食录入和编辑 | 训练数据聚合 |
| 教练 | 训练/饮食上下文和会话 | 直接改写训练历史 |
| 计划 | 计划模板和训练预填 | 伪造训练记录 |
| 身体 | 身体数据录入和趋势 | 训练容量统计 |
| 我的 | 用户资料、数据管理、导出和测试数据 | 业务数据计算 |

### 统计页当前结构

统计页只展示分析内容：

1. 训练次数、总容量、本月训练日。
2. 当前近 14 天训练量柱状图，固定 14 个日期槽位。
3. 动作估算 1RM 折线图。
4. 动作最大重量 Top 5。
5. 身体数据入口。

月历和当天详情只保留在日历页，避免两个页面展示两套日期状态。

## 6. 关键流程

### 6.1 保存训练

    record 页面输入
      -> 校验至少有一个动作和一组输入
      -> 写入 workouts
      -> 使用返回的 workouts._id 写入 sets.sessionId
      -> 页面清空当前 session

写入失败时不应展示“已保存”。训练组批量写入失败需要在日志中保留 sessionId，便于清理孤立会话。

### 6.2 日历和统计

    读取 workouts
      -> workoutData.summarize()
      -> 得到 trainedDates / 每日会话数
      -> 日历显示训练日、统计显示本月训练日

    读取 sets
      -> 通过 sessionId 关联 workouts
      -> statsData.aggregate()
      -> 得到训练容量、最大重量和 1RM

### 6.3 点击日历日期

    dateKey(workout) === selectedDate
      -> 显示当天会话摘要
      -> 按 sessionId 查询 sets
      -> historyData.buildGroups()
      -> 在当前页面展示组明细

## 7. 云端配置和部署

### 首次配置

1. 在 utils/config.js 填写真实 CLOUD_ENV。
2. 创建集合：users、workouts、sets、plans、bodyMetrics、nutritionLogs、dietPlans、agentSessions。
3. 每个集合设置为“仅创建者可读写”，并按 [docs/operations/cloudbase-indexes.md](docs/operations/cloudbase-indexes.md) 创建复合索引。
4. 用微信开发者工具编译并确认首页云连接状态正常。

### 云函数

修改以下目录后必须重新部署对应云函数：

- cloudfunctions/seedData
- cloudfunctions/exportData
- cloudfunctions/agent
- cloudfunctions/saveWorkout
- cloudfunctions/ensureUser

测试数据只用于开发验收。部署后从“我的 -> 载入测试数据”执行，成功结果应返回 24 次训练和 312 组明细。不要把测试数据逻辑带入正式用户初始化流程。

### 数据排查顺序

统计或日历不一致时按顺序检查：

1. workouts 是否有当前用户的记录。
2. 记录的 dateStr 是否为用户本地日期。
3. sets.sessionId 是否能匹配 workouts._id。
4. sets.completed 是否为 false。
5. sets.weight / sets.reps 是否为数字。
6. 集合权限和当前云环境是否一致。

## 8. 开发规则

1. 先确定数据事实来源，再写页面。
2. 日期逻辑只能放在 utils/workoutData.js。
3. 聚合逻辑只能放在纯函数模块，页面不复制聚合代码。
4. 新增字段时同步更新本文件、数据纯函数和测试。
5. 不为了填满图表伪造训练数据；无数据要显示空状态。
6. 不在页面中同时维护两套“训练日”数组。
7. WXML 只负责结构和绑定，复杂判断放 JS。
8. 新页面先复用 app.wxss 的 token 和组件。
9. 提交使用 Conventional Commits：feat、fix、refactor、test、docs、style、chore。
10. 不提交 .workbuddy、个人配置、密钥和未授权素材。

## 9. 本地验证

    node scripts/test_stats.js
    node scripts/test_history.js
    node scripts/test_plan.js
    node scripts/test_record_plan.js
    node scripts/test_record_save.js

    node --check pages/stats/stats.js
    node --check pages/calendar/calendar.js
    node --check utils/workoutData.js
    node --check utils/statsData.js

微信开发者工具验证：

1. 清缓存 -> 全部清除 -> 重新编译。
2. 保存一条 2 号训练。
3. 检查日历 2 号亮起。
4. 检查统计本月训练日同步增加。
5. 检查统计近 14 天对应日期出现容量柱。
6. 点击日历 2 号，确认训练详情与记录页一致。

## 10. 当前变更记录

### 2026-08-03 · 数据口径重整

- 新增 utils/workoutData.js，统一日期解析和训练会话摘要。
- 日历、历史、统计统一使用 workouts 派生训练日。
- 统计容量和 1RM 继续使用有效 sets，未完成组不计入指标。
- 统计页移除重复月历，改为表现分析视图。
- 训练量图改为固定 14 格柱状图，避免稀疏日期导致 canvas 误导。
- 文档改为当前架构规范，历史过程由 Git 保留。

### 2026-08-03 · 视觉与尺寸统一

- 以 `E:\AgentData\fitlog-redesign\index.html` 的 375 × 812 px 样机为基准，统一页面左右内边距、标题/正文/辅助文字字号和卡片圆角。
- 全局背景、卡片、嵌层、主色和提示色收敛到 `app.wxss` token，移除负字间距和不必要的背景光晕。
- 首页、动作库、记录、统计、日历、我的、教练页统一到同一套暗夜训练场视觉；统计坐标轴和日历日期格同步调整可读性。
- 自定义底部导航改为实体深色浮层，并在页面显示时按当前 route 同步选中态。

## 11. 上线前清单

- [ ] CLOUD_ENV 已确认是生产环境。
- [ ] 所有集合权限为仅创建者可读写。
- [ ] 记录保存、日历点击、历史详情、统计指标均已真机验证。
- [ ] 测试数据入口已隐藏或限制为开发环境。
- [ ] Agent 云函数环境变量已配置在云端。
- [ ] 导出数据不包含其他用户记录。
- [ ] 已执行全部 Node 测试。
- [ ] 已用规范提交并在 GitHub Desktop 确认变更范围。
