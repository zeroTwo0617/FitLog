# FitLog 开发文档

> 当前版本：数据与界面统一版（含主题与可读性调整）
> 更新时间：2026-08-05
> 项目形态：微信小程序原生 + 微信云开发 CloudBase

这份文档只记录当前有效的架构和开发约定。历史方案和旧提交说明不再堆在这里，改动历史以 Git 为准。

## 1. 产品边界

FitLog 是一个个人训练记录工具，核心闭环是：

1. 选择动作并记录每组次数、重量、休息。
2. 保存后形成一条训练会话。
3. 日历按训练会话展示训练日，并查看当天训练和饮食。
4. 历史按训练会话查看详情。
5. 统计基于训练会话和组明细计算训练量、最大重量和估算 1RM。
6. 教练、饮食、计划和身体数据围绕同一账号数据工作。

当前不引入独立后端。CloudBase 是唯一远端数据源，客户端只做页面查询和轻量聚合。

## 2. 技术架构

- UI：微信小程序原生 WXML / WXSS / JavaScript
- 数据：CloudBase 文档数据库
- 身份：wx.cloud.init({ traceUser: true }) 自动注入 _openid
- 权限：集合设置为“仅创建者可读写”
- 纯逻辑：放在 utils/*Data.js，不依赖 wx
- 页面：负责查询、状态管理和渲染
- 测试：Node 运行纯函数和页面 mock 测试

### 目录职责

| 目录 | 职责 |
| --- | --- |
| app.js | 云环境初始化、全局主题和页面壳 |
| app.wxss | 全局设计 token、按钮、卡片、表单和通用状态 |
| utils/config.js | 云环境 ID 和集合名唯一配置 |
| utils/cloud.js | 惰性获取数据库实例 |
| utils/workoutData.js | 训练日期和训练会话摘要的唯一口径 |
| utils/statsData.js | 训练量、最大重量、日历数据、1RM 和身体趋势 |
| utils/historyData.js | 历史排序和训练详情分组 |
| utils/nutrition.js | 饮食日期聚合和宏量营养换算 |
| utils/planData.js | 计划与记录页 session 的转换 |
| pages/ | 页面查询、交互和展示 |
| cloudfunctions/ | 服务端权限或批处理云函数 |
| scripts/ | 纯逻辑和数据生成测试 |

### 2.1 视觉与尺寸基准

参考稿以 375 × 812 px 手机画布为基准，小程序用 rpx 近似映射，页面只使用以下共享尺度：

| 项目 | 参考尺寸 | 小程序基准 |
| --- | ---: | ---: |
| 页面左右内边距 | 18 px | 36 rpx |
| 页面主标题 | 29–31 px | 58–62 rpx |
| 区块标题 | 15–16 px | 30–32 rpx |
| 正文 | 14 px | 28 rpx |
| 辅助文字 | 10–12 px | 20–24 rpx |
| 底部导航文字 | 9 px | 18 rpx |
| 记录输入格 / 主按钮 | 40–50 px | 80–100 rpx |

视觉 token 统一维护在 `app.wxss`：浅色默认使用浅灰背景、白色卡片和深绿色主色，`.theme-dark` 覆盖为深色背景、深色卡片和高亮绿色。页面根节点使用 `theme-{{theme}}` 类驱动切换。页面 WXSS 不应重新定义同名颜色或使用负字间距；标题、正文、辅助信息优先复用全局类和 token。SVG 图标提供深色原版与浅色 `-light` 版，按主题切换 `src`。

当前全局基础字号为 `28rpx`，`.h-title` 为 `58rpx`，首页主标题为 `62rpx`，区块标题为 `31–32rpx`。动作名、计划名、Agent 消息和饮食名称使用 `27–35rpx`，辅助信息使用 `20–24rpx`。底部导航文字保持 `18rpx`，不随页面正文放大，避免导航高度变化。完整层级和验收要求见 [docs/architecture/ui-design-system.md](docs/architecture/ui-design-system.md)。

底部导航是实体浮层，不使用透明白底。`custom-tab-bar` 由于样式隔离使用显式的浅色/深色类，页面切换和主题切换时都根据当前 route 与主题同步选中项和颜色。

## 3. 数据模型

### 3.1 通用约定

- 每条用户数据由 CloudBase 自动写入 _openid。
- 页面不得使用另一个用户 ID 过滤数据。
- 日期展示统一使用 YYYY-MM-DD。
- dateStr 是用户本地训练日的权威字段。
- date / createdAt 是时间戳，用于排序和审计，不替代 dateStr。
- 集合名只能从 utils/config.js 的 COLLECTIONS 读取。

### 3.2 workouts：训练会话事实表

一条文档代表一次训练：

    {
      _id: 'session-id',
      _openid: 'owner-openid',
      date: Date,
      dateStr: '2026-08-02',
      title: '练背日',
      planId: '',
      exercises: [
        { exerciseId: 'deadlift', name: '传统硬拉', setCount: 3 }
      ],
      setTotal: 3,
      createdAt: Date
    }

职责：

- 决定训练次数。
- 决定哪一天算训练日。
- 提供日历和历史列表的会话摘要。
- 通过 _id 与 sets.sessionId 关联。

### 3.3 sets：训练组明细表

一条文档代表一次训练中的一组：

    {
      _id: 'set-id',
      _openid: 'owner-openid',
      sessionId: 'session-id',
      exerciseId: 'deadlift',
      exerciseName: '传统硬拉',
      setIndex: 1,
      reps: 5,
      weight: 100,
      restSec: 120,
      completed: true,
      createdAt: Date
    }

职责：

- 计算训练容量：reps * weight。
- 计算动作最大重量。
- 计算 Epley 估算 1RM。
- completed === false 的组不计入指标。
- 缺少 completed 的旧数据按已完成处理。
- 没有对应 workouts._id 的孤立组不计入统计。

### 3.4 其他集合

| 集合 | 关键字段 | 用途 |
| --- | --- | --- |
| nutritionLogs | dateStr、mealType、protein、carbs、fat、calories | 每日饮食 |
| dietPlans | dailyTarget.calories | 饮食目标 |
| bodyMetrics | dateStr、weight、fatPct、height | 身体数据 |
| plans | name、items[] | 训练计划 |
| users | _openid、nickName、goal、level | 用户档案 |
| agentSessions | 会话消息和模式 | 教练会话 |

## 4. 唯一数据口径

### 4.1 日期口径

所有页面调用 utils/workoutData.dateKey(record)：

1. 优先读取合法的 record.dateStr。
2. 没有 dateStr 时兼容 date。
3. 仍没有时兼容 createdAt。
4. 无法解析的记录不进入日期统计。

禁止页面直接写 item.dateStr 来推导训练日。

### 4.2 训练日口径

训练日只由 workouts 决定。

因此：

- 用户保存了一次训练，即使所有组都没有勾选完成，该日期仍会出现在日历和统计的训练日计数。
- 该训练的容量和 1RM 可以为 0，因为这些指标需要有效的已完成组。
- 日历、首页打卡、统计本月训练日和历史训练日数必须使用 workoutData.summarize(workouts)。

### 4.3 指标口径

指标由 workouts + sets 联合计算：

1. 用 workouts._id 映射 sets.sessionId。
2. 过滤 completed === false。
3. 过滤无效的次数、重量和动作。
4. 再计算容量、最大重量和 1RM。
5. 不允许用 workout 摘要里的 setTotal 计算容量。

## 5. 页面职责

| 页面 | 负责 | 不负责 |
| --- | --- | --- |
| 首页 | 今日状态、快捷入口 | 自己重新聚合训练日期 |
| 记录 | 编辑当前训练并写入 workouts、sets | 统计历史指标 |
| 日历 | 月历、某天训练详情、当天饮食 | 计算训练容量 |
| 历史 | 会话列表和详情 | 推导新的统计口径 |
| 统计 | 训练次数、训练量、最大重量、1RM | 重复展示月历详情 |
| 饮食 | 某天饮食录入和编辑 | 训练数据聚合 |
| 教练 | 训练/饮食上下文和会话 | 直接改写训练历史 |
| 计划 | 计划模板和训练预填 | 伪造训练记录 |
| 身体 | 身体数据录入和趋势 | 训练容量统计 |
| 我的 | 用户资料、数据管理 | 业务数据计算 |
| 设置 | 主题开关和本地显示偏好 | 业务数据写入 |

### 统计页当前结构

统计页只展示分析内容：

1. 训练次数、总容量、本月训练日。
2. **动作最大重量 Top 5**（放在第一个区块）。
3. 近 7 天训练量柱状图，7 个日期槽位均分铺满，横轴标签显示「月/日」；点击柱子浮现当日容量，再点收起。
4. 动作估算 1RM 折线图，只展示三大项（卧推/深蹲/传统硬拉），按最大重量降序、重项在前并默认选中；横轴同样为 7 天、全标签。
5. 身体数据入口。

月历和当天详情只保留在日历页，避免两个页面展示两套日期状态。

## 6. 关键流程

### 6.1 保存训练

    record 页面输入
      -> 校验至少有一个动作和一组输入
      -> 写入 workouts
      -> 使用返回的 workouts._id 写入 sets.sessionId
      -> 页面清空当前 session

写入失败时不应展示“已保存”。训练组批量写入失败需要在日志中保留 sessionId，便于清理孤立会话。

### 6.2 日历和统计

    读取 workouts
      -> workoutData.summarize()
      -> 得到 trainedDates / 每日会话数
      -> 日历显示训练日、统计显示本月训练日

    读取 sets
      -> 通过 sessionId 关联 workouts
      -> statsData.aggregate()
      -> 得到训练容量、最大重量和 1RM

### 6.3 点击日历日期

    dateKey(workout) === selectedDate
      -> 显示当天会话摘要
      -> 按 sessionId 查询 sets
      -> historyData.buildGroups()
      -> 在当前页面展示组明细

### 6.4 主题与字体

    我的 -> 应用 -> 设置 -> 切换黑色主题
      -> theme.setTheme(theme)
      -> 保存本地主题
      -> 更新当前页面根节点、微信顶栏和自定义底部导航

主题默认使用浅色。新增页面必须在根节点添加 `theme-{{theme}}`，样式优先使用 `app.wxss` 的 token。字号调整应遵循 [视觉与字体设计系统](docs/architecture/ui-design-system.md)，并检查固定输入格、底部按钮、日历格和导航栏是否保持稳定。

## 7. 云端配置和部署

### 首次配置

1. 在 utils/config.js 填写真实 CLOUD_ENV。
2. 创建集合：users、workouts、sets、plans、bodyMetrics、nutritionLogs、dietPlans、agentSessions、agentUploads。
3. 每个集合设置为“仅创建者可读写”，并按 [docs/operations/cloudbase-indexes.md](docs/operations/cloudbase-indexes.md) 创建复合索引。
4. 用微信开发者工具编译并确认首页云连接状态正常。

### 云函数

修改以下目录后必须重新部署对应云函数：

- cloudfunctions/agent
- cloudfunctions/saveWorkout
- cloudfunctions/ensureUser
- cloudfunctions/savePlan
- cloudfunctions/saveNutritionLog
- cloudfunctions/saveBodyMetric
- cloudfunctions/deletePlan
- cloudfunctions/deleteNutritionLog
- cloudfunctions/deleteUserData
- cloudfunctions/updateUserActive
- cloudfunctions/getDayDetail

**更新/删除云函数约定**：按 `_openid` 过滤后执行 `update`/`remove`，必须检查返回的 `stats.updated` / `stats.removed`，为 0 时返回 `NOT_FOUND`，避免 ID 不存在或越权时「假成功」。

**日期校验约定**：`dateStr` 是用户选择的业务日期，除格式 `YYYY-MM-DD` 外必须校验真实性（月份/日期合法、含闰年归一化拦截）并**拒绝未来日期**；`createdAt` 使用云函数服务端时间。

**写入白名单约定**：云函数接收客户端数据后，必须**显式构造允许字段**再写入数据库，不得 `Object.assign({}, event, ...)` 透传客户端字段（会带入 `_id`、`createdAt` 等未知字段，存在污染与越权风险）。

**并发写入约定**：对同一文档做「读-改-写」（如追加数组）必须用事务（`runTransaction` 内重新读最新值再写）或版本号条件更新，避免并发请求各自基于旧值互相覆盖；创建用 `add` 无需事务。

**每天一条约定**：`bodyMetrics` 是「每天一条」的数据模型，`saveBodyMetric` 按 `{ dateStr, _openid }` Upsert（存在覆盖、不存在新建，事务保证不产生同日重复）；前端展示对存量重复按 `updatedAt`/`createdAt` 取最新去重。

### 数据排查顺序

统计或日历不一致时按顺序检查：

1. workouts 是否有当前用户的记录。
2. 记录的 dateStr 是否为用户本地日期。
3. sets.sessionId 是否能匹配 workouts._id。
4. sets.completed 是否为 false。
5. sets.weight / sets.reps 是否为数字。
6. 集合权限和当前云环境是否一致。

## 8. 开发规则

1. 先确定数据事实来源，再写页面。
2. 日期逻辑只能放在 utils/workoutData.js。
3. 聚合逻辑只能放在纯函数模块，页面不复制聚合代码。
4. 新增字段时同步更新本文件、数据纯函数和测试。
5. 不为了填满图表伪造训练数据；无数据要显示空状态。
6. 不在页面中同时维护两套“训练日”数组。
7. WXML 只负责结构和绑定，复杂判断放 JS。
8. 新页面先复用 app.wxss 的 token 和组件。
9. 提交使用 Conventional Commits：feat、fix、refactor、test、docs、style、chore。
10. 不提交 .workbuddy、个人配置、密钥和未授权素材。

## 9. 本地验证

    node scripts/test_stats.js
    node scripts/test_history.js
    node scripts/test_plan.js
    node scripts/test_record_plan.js
    node scripts/test_record_save.js

    node --check pages/stats/stats.js
    node --check pages/calendar/calendar.js
    node --check utils/workoutData.js
    node --check utils/statsData.js

微信开发者工具验证：

1. 清缓存 -> 全部清除 -> 重新编译。
2. 保存一条 2 号训练。
3. 检查日历 2 号亮起。
4. 检查统计本月训练日同步增加。
5. 检查统计近 14 天对应日期出现容量柱。
6. 点击日历 2 号，确认训练详情与记录页一致。

## 10. 当前变更记录

### 2026-08-05 · 主题设置与字体可读性调整

- 新增设置页，支持浅色/深色主题切换、本地持久化、微信顶栏和自定义底部导航同步。
- 参照 `design/fitlog-ui-prototype.html` 放大整体字体：基础字号 `28rpx`，页面标题 `58–62rpx`，重点内容 `27–35rpx`，导航文字保持 `18rpx`。
- 新增 [docs/architecture/ui-design-system.md](docs/architecture/ui-design-system.md)，统一记录视觉层级、主题和样式职责。
- 静态验证通过：JavaScript、JSON、WXSS 花括号和 Git 空白检查。

### 2026-08-03 · 日历详情消除 N+1 查询

- 新增 `getDayDetail` 云函数：一次返回某天全部训练 + 全部组明细（`sessionId in` 按批查）。
- 日历页 `tapDay`、day-detail 页从逐条查 sets 改为一次 `workoutRepo.dayDetail(dateStr)`，消除 N+1。
- day-detail 页不再全量 `listAll()` 筛日期。
- 开发文档 §7 云函数清单新增 `getDayDetail`。

### 2026-08-03 · 移除 seedData 云函数

- 删除 `cloudfunctions/seedData/`、`scripts/test_seed.js`、前端「载入测试数据」入口（mine.js `loadSeed` + wxml 菜单行）、`repositories/system.js` 的 `seedData()`。
- 同步清理文档引用（data-access、v1-release-plan、开发文档 §7 云函数清单）。
- 原因：seedData 按 openid 删除 `isSeed:true` 数据，存在越权与生产数据污染风险。

### 2026-08-03 · 数据口径重整

- 新增 utils/workoutData.js，统一日期解析和训练会话摘要。
- 日历、历史、统计统一使用 workouts 派生训练日。
- 统计容量和 1RM 继续使用有效 sets，未完成组不计入指标。
- 统计页移除重复月历，改为表现分析视图。
- 训练量图改为固定 14 格柱状图，避免稀疏日期导致 canvas 误导。
- 文档改为当前架构规范，历史过程由 Git 保留。

### 2026-08-03 · 数据访问修复与统计页优化

- **修复 getAll 分页 bug**（`utils/cloud.js`）：小程序端 SDK 单次 `get()` 上限 20 条，原实现误用「返回条数 < 请求条数」判断，导致只读第一页、超过 20 条后靠后的记录被静默截断。修复后用「是否满页」决定翻页。
- **getAll 并行分页**：从逐页串行改为每批 5 页并行读，大数据量下加载提速，历史/日历/统计/身体/营养全部受益。
- **统计页展示**：训练量窗口 14 → 7 天、横轴标签全标并带月份（`7/28`、`8/1`）；1RM 指标过滤为三大项（卧推/深蹲/传统硬拉），排除罗马尼亚硬拉等辅助变体，按最大重量降序、重项在前并默认选中；「动作最大重量」区块移到第一位。
- **统计页交互**：点击训练量柱子浮现当日容量，再点收起；0 容量日期不显示柱子；柱列从固定 `7.14%` 改为 flex 均分铺满。
- 变更记录文档化：新增 `docs/changelog.md` 归档本次修复与优化。

### 2026-08-03 · 浅色主题配色完善

- **根因**：`page { background: var(--bg) }` 在 `page` 作用域解析变量，`.theme-light` 加在页面内层根 view 上，反哺不到祖先 `page`，浅色下页面底仍是深黑。修复：`.page` 负责渲染主题底色。
- **浅色配色**：延续绿色设计语言做日间反转，主色荧光绿转深绿 `#3F6B0F`、强调色转深橙 `#C2410C`、浅底 `#F6F7F2`；顶栏浅色黑字浅底。
- **硬编码清理**：`color:#12160a` 批量换 `var(--on-primary)`；白斑马纹换 `var(--surface-2)`；固定 Volt 渐变补 `.theme-light` 柔和版；anim-figure 舞台补浅色覆盖。
- **SVG 图标**：生成 16 个 `-light.svg` 深色版，WXML 按 `theme === 'light'` 拼接 `-light` 后缀。
- **底部导航栏**：custom-tab-bar 样式隔离导致 `var(--*)` 解析失败，改显式颜色 + `.theme-light` 覆盖；`theme.setTheme` 增加 `getTabBar()` 广播，切主题即时跟随。
- **顶栏**：`syncNavBar` 去掉缓存，每次 onShow 强制设置导航栏颜色。

### 2026-08-03 · 云函数更新/删除假成功修复

- `savePlan`、`saveNutritionLog` 更新后检查 `stats.updated`，为 0 返回 `NOT_FOUND`。
- `deletePlan`、`deleteNutritionLog` 删除后检查 `stats.removed`，为 0 返回 `NOT_FOUND`。
- 前端经 `utils/cloud.js` 的 `callFunction` 抛错，`.catch` 正常处理。
- 开发文档 §7 云函数清单补全，并新增「更新/删除需检查影响条数」约定。

### 2026-08-03 · 云函数日期真实性与未来日期校验

- `saveWorkout`、`saveBodyMetric`、`saveNutritionLog`、`agent` 的 `dateStr` 从仅格式校验升级为真实日期校验：拦截 `2026-99-99`、`2026-02-30`、非闰年 `2/29` 等，并拒绝未来日期。
- 云函数独立部署无法跨目录 require，`isDateStr` 在各函数内联。
- 开发文档 §7 新增「日期校验约定」。

### 2026-08-03 · saveNutritionLog 字段白名单化

- `saveNutritionLog.normalize` 从 `Object.assign({}, event)` 透传改为显式构造白名单字段，与 agent `validateMeal` 对齐。
- 验证：恶意输入（`_id`/`_openid`/`createdAt`/`mealLabel`）不进入输出。
- 开发文档 §7 新增「写入白名单约定」。

### 2026-08-03 · agent 会话并发覆盖修复

- `saveSession` 从「读-改-写」改为事务：会话存在时 `runTransaction` 内重新读最新 `messages` 再追加写入，防并发覆盖；最多重试 3 次。
- 模拟验证：旧方案并发丢消息，事务方案两条都保留。
- 开发文档 §7 新增「并发写入约定」。

### 2026-08-03 · 身体数据每天一条 Upsert

- `saveBodyMetric` 从每次 `add` 改为按 `{ dateStr, _openid }` 事务 Upsert：同一天重复保存覆盖当天记录，不再产生同日多条。
- body 页对存量重复按 `updatedAt`/`createdAt` 去重，列表与趋势图稳定。
- 开发文档 §7 新增「每天一条约定」。

### 2026-08-03 · 视觉与尺寸统一

- 以 `E:\AgentData\fitlog-redesign\index.html` 的 375 × 812 px 样机为基准，统一页面左右内边距、标题/正文/辅助文字字号和卡片圆角。
- 全局背景、卡片、嵌层、主色和提示色收敛到 `app.wxss` token，移除负字间距和不必要的背景光晕。
- 首页、动作库、记录、统计、日历、我的、教练页统一到同一套暗夜训练场视觉；统计坐标轴和日历日期格同步调整可读性。
- 自定义底部导航改为实体深色浮层，并在页面显示时按当前 route 同步选中态。

## 11. 上线前清单

- [ ] CLOUD_ENV 已确认是生产环境。
- [ ] 所有集合权限为仅创建者可读写。
- [ ] 记录保存、日历点击、历史详情、统计指标均已真机验证。
- [ ] 测试数据入口已移除（`seedData` 云函数已删除）。
- [ ] Agent 云函数环境变量已配置在云端。
- [ ] 浅色/深色主题切换后页面底色、顶栏、底部导航和图标同步。
- [ ] 放大后的字体在首页、动作库、教练、训练记录和饮食页面不溢出或遮挡操作。
- [ ] 已执行全部 Node 测试。
- [ ] 已用规范提交并在 GitHub Desktop 确认变更范围。
