# FitLog 训练记录本 · 微信小程序开发文档（PRD v0.1）

> 状态：Phase 0 完成，功能 #1（项目骨架 + 接入预设动作库）已落地
> 最后更新：2026-07-11
> 维护人：高级开发工程师（Senior Developer）
> 文档规则：每个功能开发完成后必须更新本文档（进度 / 决策 / 待确认项）

---

## 1. 项目概述

| 项 | 内容 |
| --- | --- |
| 项目名称（暂定） | **FitLog 训练记录本** |
| 形态 | 微信小程序（原生）+ 微信云开发后端 |
| 一句话定位 | 用结构化方式记录每一次训练的「动作-组数-次数-重量」，让进步看得见 |
| 目标用户 | 有规律的健身训练者（新手到进阶），希望量化追踪力量/围度进步的人 |
| 核心价值 | 记录像发朋友圈一样简单，趋势、打卡、计划一站式沉淀训练生涯 |

### 设计基调（premium 取向）
- 录入流程极简、大按钮、可单手操作，训练中不打断节奏
- 微交互：保存成功 toast、数字滚动、组间休息计时圆环、震动反馈
- 浅色 / 深色双主题，跟随系统并可手动切换
- 性能目标：首屏 < 1.5s，历史列表虚拟滚动，动画 60fps

---

## 2. 需求确认结论（已与产品方确认）

| 维度 | 结论 |
| --- | --- |
| 记录形式 | **结构化记录（组/次/重）**，并兼容计时 / 距离类动作的混合记录 |
| 动作来源 | **内置预设动作库**（系统维护）+ 用户自定义动作 |
| 账号与同步 | **微信授权登录 + 云端同步**（多设备可用，换手机不丢数据） |
| 增强功能 | 全选：训练计划/模板、数据统计与趋势图、打卡日历、身体数据记录 |

---

## 3. 功能清单与优先级（MoSCoW）

### P0 · MVP（必须，先上线）
- [ ] 微信授权登录（wx.login → openid），首次进入引导授权
- [x] 内置动作库：分类浏览（胸/背/腿/肩/臂/核心/有氧）、搜索、选择（已落地：本地预设 JSON 1324 条，见 §5.1 / §6 / §12）
- [ ] 新建训练：选动作 → 逐组录入（次数 / 重量 / 休息秒）→ 保存
- [ ] 训练历史列表 + 训练详情（按日期倒序）
- [ ] 云端存储与多设备同步（云数据库）

### P1 · 核心增强（上线后紧接）
- [ ] 训练计划/模板：保存常用动作组合（练胸日/练腿日），一键「开始训练」
- [ ] 数据统计：训练量、单动作最大重量、训练频率的趋势折线/柱状图
- [ ] 打卡日历：按日期标记有训练的天（可由训练记录自动派生）
- [ ] 身体数据记录：体重、体脂、胸/腰/臂/腿围度，与时间轴对照

### P2 · 体验打磨（锦上添花）
- [ ] 组间计时器 + 休息结束震动提醒
- [ ] 自动带出「上次重量 / 上次次数」快速 +1 录入
- [ ] 深色模式完整适配
- [ ] 训练数据导出（JSON / 图片分享卡片）

### 暂未纳入（留扩展位）
- 社交 / 好友 / 排行榜（本期不做，架构预留 userId 关联）
- 多平台（App / Web）：本期仅微信小程序

---

## 4. 信息架构（页面结构）

```
首页 (index)
 ├─ 今日概览 + 「快速开始训练」入口
 ├─ 打卡日历缩略（本月训练热力）
 └─ 快捷入口：动作库 / 我的计划 / 数据

记录训练 (record)
 ├─ 步骤1：选择动作（动作库弹层，支持多选）
 ├─ 步骤2：逐动作逐组录入（次数/重量/休息）
 └─ 步骤3：保存（标题/时长/备注可选）

动作库 (exercises)
 ├─ 分类 Tab + 搜索
 └─ 动作详情（默认单位、是否系统库）

我的计划 (plans)
 ├─ 计划列表 / 新建计划（动作组合模板）
 └─ 计划详情 → 「开始训练」跳转 record

数据 (stats)
 ├─ 趋势图（训练量/最大重量/频率）
 └─ 身体数据曲线

身体数据 (body)
 ├─ 录入表单 + 历史曲线

我的 (profile)
 ├─ 账号信息、主题切换、关于、导出
```

---

## 5. 数据模型（微信云开发 · 文档型数据库）

> 说明：云开发数据库为 NoSQL 文档库，以下每个「集合」对应一张表。

### 5.1 `exercises` 动作库
| 字段 | 类型 | 说明 |
| --- | --- | --- |
| `_id` | string | 主键 |
| `name` | string | 动作名，如「杠铃深蹲」 |
| `category` | string | 胸/背/腿/肩/臂/核心/有氧 |
| `defaultUnit` | string | `weight`(负重) / `bodyweight`(自重) / `timed`(计时) / `distance`(距离) |
| `createdBy` | string | `system` 或用户 openid（自定义动作） |
| `isCustom` | bool | 是否用户自建 |

### 5.2 `training_sessions` 训练记录（一次训练）
| 字段 | 类型 | 说明 |
| --- | --- | --- |
| `_id` | string | 主键 |
| `userId` | string | 微信 openid |
| `date` | date | 训练日期 |
| `title` | string | 可选标题，如「练腿日」 |
| `planId` | string? | 若由计划发起则关联 |
| `durationMin` | int? | 总时长（分钟） |
| `note` | string? | 备注 |
| `createdAt` | date | 创建时间 |

### 5.3 `sets` 组明细
| 字段 | 类型 | 说明 |
| --- | --- | --- |
| `_id` | string | 主键 |
| `sessionId` | string | 关联 training_sessions._id |
| `exerciseId` | string | 关联 exercises._id |
| `exerciseName` | string | 冗余存名，避免联表 |
| `setIndex` | int | 第几组（从 1 起） |
| `reps` | int? | 次数（计时类存 durationSec） |
| `weight` | number? | 重量（kg），自重为 0 |
| `restSec` | int? | 组间休息秒 |
| `completed` | bool | 是否完成 |

> 设计取舍：组明细独立成集合而非内嵌数组，便于按动作做聚合统计（趋势图查询高效）。

### 5.4 `plans` 训练计划模板
| 字段 | 类型 | 说明 |
| --- | --- | --- |
| `_id` | string | 主键 |
| `userId` | string | 创建者 openid |
| `name` | string | 计划名 |
| `items` | array | `[{exerciseId, exerciseName, targetSets, targetReps, targetWeight}]` |

### 5.5 `body_records` 身体数据
| 字段 | 类型 | 说明 |
| --- | --- | --- |
| `_id` | string | 主键 |
| `userId` | string | openid |
| `date` | date | 记录日期 |
| `weight` | number? | 体重 kg |
| `fatPct` | number? | 体脂 % |
| `chest/waist/arm/thigh` | number? | 围度 cm |
| `note` | string? | 备注 |

### 5.6 `users` 用户档案
| 字段 | 类型 | 说明 |
| --- | --- | --- |
| `openid` | string | 主键 |
| `nickname` | string? | 昵称 |
| `avatar` | string? | 头像 URL |
| `createdAt` | date | 注册时间 |

### 5.7 打卡日历
- **不单独建表**：由 `training_sessions` 按 `date` 去重聚合得到「有训练的日子」，前端渲染日历标记。

---

## 6. 技术选型

| 层 | 选型 | 理由 |
| --- | --- | --- |
| 前端 | 微信小程序原生框架（WXML / WXSS / JS） | 微信登录与生态最顺，无需额外构建链 |
| 后端 | **微信云开发 CloudBase** | 免运维服务器，天然微信登录，免费额度覆盖 MVP |
| 数据库 | 云开发文档数据库（NoSQL） | 与小程序 SDK 直连，开发快 |
| 云函数 | 云函数（需聚合/权限隔离时用） | 复杂统计、数据导出、权限收口 |
| 存储 | 云存储 | 头像、分享卡片等文件 |
| 登录 | `wx.login` + `wx.getUserProfile` → openid | 官方标准流程 |
| UI | 自绘组件 + 轻量工具函数 | 统一设计 token（间距/圆角/主色），保证 premium 质感 |

**备选方案**：若未来需要跨端（App / H5），可迁移到 uni-app / Taro，当前没必要。
**预设动作库**：采用 `fitness-exercise-library` 技能（1324 个动作，含中文分步做法），经 `scripts/build_exercise_preset.py` 精简为 `data/exercises.preset.js`（648KB，本地 JS 模块，`module.exports` 数组，离线可用）。首期方案为「本地精简数据包」（见 §12），后续可按需 seed 进云 `exercises` 集合。**注意**：微信小程序 `require` 双扩展名 `.json` 会解析失败（报 `module '...json.js' is not defined`），故用 `.js` 模块承载数据。

---

## 7. 关键交互流程（MVP 录入）

```
首页「快速开始」→ 选动作(可多选) → 进入录入
 → 对每个动作：默认带出上次重量/次数 → 逐组填 reps/weight/rest
 → 可「+ 添加一组」「完成该动作」
 → 全部动作完成 → 填标题/时长/备注(可选) → 保存
 → toast「已保存」→ 返回首页，日历标记 +1
```

优化点（P2）：组间计时圆环 + 休息结束震动；「复制上次训练」一键重建。

---

## 8. 开发阶段规划

| 阶段 | 内容 | 产出 |
| --- | --- | --- |
| Phase 0 | 项目脚手架 + git 仓库 + 云开发环境申请 + 设计 token | 可运行空壳 |
| Phase 1 | 登录 + 动作库 + 结构化记录（P0） | 可用 MVP |
| Phase 2 | 历史/详情 + 云端同步校验 | MVP 闭环 |
| Phase 3 | 计划/模板 + 数据趋势 + 打卡日历 + 身体数据（P1） | 完整版 |
| Phase 4 | 计时器 / 深色模式 / 导出（P2） | 体验打磨 |

---

## 9. Git 工作流规范

- **仓库**：本项目独立仓库（`fitness-miniprogram`），不混入其他文件
- **分支模型**：
  - `main`：稳定可运行版本
  - `dev`：日常集成分支
  - `feature/<功能>`：按功能开分支（如 `feature/record-flow`）
- **提交规范**（Conventional Commits）：
  - `feat:` 新功能 / `fix:` 修复 / `docs:` 文档 / `style:` 样式 / `refactor:` 重构 / `test:` 测试 / `chore:` 杂项
  - 例：`feat(record): 新增逐组录入与保存流程`
- **合并**：功能分支 → `dev`，评审通过后合 `main`；禁止直接推 `main`
- **忽略项**：`node_modules/`、`miniprogram_npm/`、`cloudfunctions/**/node_modules/`、本地私钥、`*.log`、编辑器目录
- **首个 commit**：`docs: 初始化项目与开发文档(PRD v0.1)`

---

## 10. 待确认 / 假设项

1. **小程序 AppID**：是否已在微信公众平台注册？未注册则先用「测试号」跑通，上线前补 AppID。
2. **预设动作库**：首批分类与条目由开发方整理，产品方确认/增删。
3. **社交/分享**：本期不做，但「训练分享卡片」（图片）在 P2 考虑。
4. **云开发环境**：地域与配额后续按实际申请；MVP 用默认免费环境。
5. **数据隐私**：身体数据属敏感信息，仅本人可见，云函数做权限收口。

---

## 11. 风险与取舍

| 风险 | 应对 |
| --- | --- |
| 云开发免费额度限制 | MVP 足够；规模化后再评估自建后端或升配 |
| 训练中结构化录入易打断节奏 | 「默认上次重量」+ 快速 +1 + 大按钮 + 计时器优化 |
| 趋势图聚合查询性能 | 组明细独立集合 + 云函数聚合，必要时预计算周/月汇总 |
| 多设备冲突 | 以云端为权威，打开时拉取最新；编辑加版本号（后续） |

---

_下一步：功能 #2 微信登录 + 云端同步（任务 #6）。_

---

## 12. 开发进度 / 更新日志

> **文档规则**：每个功能开发完成后必须更新本节（含自测结论与 git 提交号），并同步勾选 §3 功能清单、必要时更新 §10 待确认项。

### 2026-07-11 · 功能 #1 完成 — 项目骨架 + 接入预设动作库
- **提交**：`da28688`（已 push 至 GitHub `main`）
- **本次范围**（对应 §8 Phase 0 + 部分 P0「内置动作库」）：
  - 小程序空壳：`app.json / app.js / app.wxss / project.config.json / sitemap.json`，深色 premium 主题 token（主色蓝紫渐变、圆角、间距变量）
  - 数据转换：`scripts/build_exercise_preset.py` 将技能 1324 条数据精简为 `data/exercises.preset.js`（648KB，JS 模块）
  - 数据层：`utils/exerciseData.js`（分类 / 搜索 / 中文标签映射：部位·器械·目标肌群）
  - 页面：`pages/index`（首页 + 入口）、`pages/exercises`（动作库浏览 / 分类 / 搜索 / 展开中文做法）
- **自测结论**：node 语法检查（4 个 JS 全过）、JSON 校验（6 配置 + 预设全过）、筛选逻辑实测（chest→163、bench press→32，中文标签正确）
- **待产品方在微信开发者工具验证**：导入项目浏览 UI、动作库搜索 / 筛选 / 展开做法
- **未做（留给后续功能）**：微信登录、云端写入、结构化录入流程
- **决策记录**：动作库采用「本地精简数据包」方案（非云端 seed），包体控制在 2MB 内，MVP 离线可用；动作名暂用英文（数据源原名），中文仅分步做法，是否加中文译名待产品方拍板。

### 2026-07-11 · 修复 — 微信端 require 双扩展名 JSON 崩溃
- **现象**：开发者工具编译后报错 `module 'data/exercises.preset.json.js' is not defined`，页面白屏。根因是微信小程序模块系统对 `exercises.preset.json`（双扩展名）解析异常，强行补 `.js` 后缀导致找不到模块。Node 环境自测无法复现此问题。
- **修复**：构建脚本改为输出 `data/exercises.preset.js`（`module.exports = [...]` 的 JS 模块）；`utils/exerciseData.js` 改为 `require('../data/exercises.preset.js')`；删除旧的 `exercises.preset.json`。
- **提交**：337fd13
- **经验**：微信小程序内 `require` 数据，优先用 `.js` 模块而非 `.json`，尤其避免 `xxx.preset.json` 这类多扩展名文件。

### 2026-07-11 · 功能 #1.5 完成 — 动作中文译名（离线词典 + 规则组合）
- **需求决策**：产品方选定「离线词典 + 规则组合」生成中文名，列表仅显示中文名（覆盖不到的回退英文原名，避免空白）。
- **实现**：`scripts/build_exercise_preset.py` 新增 `translate()`——健身术语中英词典（部位/器械/目标肌群/修饰/动作短语）+ 规则组合（器械前缀 + 修饰 + 动作 + 部位/目标后缀）；`data/exercises.preset.js` 每条新增 `nameZh` 字段；`utils/exerciseData.js` 装饰层加 `nameZh` 且搜索兼容中英文；`pages/exercises` 列表改显示 `nameZh`、搜索框提示改为「中文/英文均可」。
- **自测结论**：node 语法检查通过；中文译名覆盖 **1134/1324（85.6%）**；中文搜索（卧推/弯举/深蹲）正常；修复了一处「器械重复」（如「哑铃哑铃划船」）的 bug（fallback 路径器械 token 与动作模板内 `e` 重复）。
- **提交**：32ba824
- **备注**：剩余 ~14% 为生僻/无法规则化的动作，显示为英文原名；词典可后续在 `build_exercise_preset.py` 顶部增删词条来提升覆盖。

### 2026-07-11 · 功能 #1.5 补完 — 中文译名覆盖率 85.6% → 99.9%
- **触发**：产品方反馈「仍有小部分未汉化」，要求仔细检查与测试。
- **根因**：三类问题——① 连字符（`push-up`/`pull-up`/`chin-up`/`sit-up` 模板用空格写，正则匹配不上连字符形式）；② 复数（`curls`/`dips`/`kicks`/`stretches` 正则 `\bcurl\b` 匹配不了复数）；③ 词典缺失（体操/瑜伽/有氧/举重/器械 lever 系列/拳击/药球投掷等大量专有动作无词条，如 planche、front lever、mountain climber、dead bug、stretch、muscle up、power clean、tire flip、ski ergometer 等）。
- **修复**：`build_exercise_preset.py` 重构 `translate()`——① 新增 `normalize()` 归一化（连字符转空格、去括号/分数/with 后缀、去孤立数字）；② `plural()`/`plural_phrase()` 复数容错（含 ch/sh/x/y 不规则）；③ 新增 `SPECIAL` 专有名词表（体操/瑜伽/有氧/举重/投掷，整体短语优先匹配，key 也归一化）；④ 补齐 MOVEMENTS（pulldown/shrug/calf/snatch/flip/slam/throw/catch/hook/tilt/tap/reach/step/hop/sprint 等）与 MOD（front/parallel/gripless/rotary/overhead/twin handle/quick/vertical 等）；⑤ MOD 匹配改用 `normalize(k)` 以兼容 `one-arm`→`one arm`；⑥ 去除俯卧撑/引体/仰卧起坐/卷腹/耸肩/臂屈伸模板里冗余的 `{b}`/`{t}` 部位前缀，译名更地道。
- **自测结论**：node 语法检查通过；中文译名覆盖 **1323/1324（99.9%）**；**仅 `spell caster`（极生僻腹肌理疗变式，无通行中文译名）回退英文原名**；重复器械 0 条；中文搜索（俯卧撑49/引体31/仰卧起坐18/卷腹36/耸肩12/下拉39/弯举179/深蹲75/拉伸55/双力臂5）均正常；英文搜索仍可用。
- **提交**：本次提交
- **经验**：微信小程序离线译名「规则组合」方案，归一化（连字符/复数/括号/数字）+ SPECIAL 优先级 + MOD key 归一化是稳定覆盖的关键；极生僻动作保留英文原名优于编造译名。

### 2026-07-11 · 功能 #2 完成 — 微信登录 + 云端同步（基础设施闭环）
- **本次范围**（对应 P0「微信授权登录 + 云端同步」基础设施）：
  - 云初始化：`app.js` 在 `onLaunch` 调 `wx.cloud.init({ env: config.CLOUD_ENV, traceUser: true })`
  - 集中配置：`utils/config.js`（APP_ID / `CLOUD_ENV` 占位 / 六个集合名常量）
  - 云库封装：`utils/cloud.js`（惰性获取 db 实例、集合引用）
  - 登录态 + 用户档案：`utils/auth.js`——采用「微信云开发免登录模式」，记录由云环境自动注入 `_openid`，配合集合权限「仅创建者可读写」实现仅本人可见；`ensureUser()` 查/建 users 集合档案，`updateActive()` 更新活跃时间
  - 首页状态卡片：`pages/index` 新增云端连接状态卡片（pending/ok/error 三态 + 档案创建时间 + 失败重试按钮），作为「云端链路是否通」的可视化验证点
- **未做（留给后续功能）**：昵称/头像授权展示、训练记录实际写入（功能 #3）、计划/统计/身体数据（#5/#6）
- **自测结论**：node 语法检查（app.js / utils/* / pages/* 全部通过）；逻辑冒烟测试（mock 微信环境，验证 `ensureUser` 三分支：[已有档案→返回不创建]、[无档案→创建后返回 created:true]、[网络错误→reject 并提示]，结果 `SMOKE_OK`）
- **需你在微信开发者工具完成的两步配置（未填则首页状态卡片显示连接失败）**：
  1. **填云环境 ID**：编辑 `utils/config.js`，将 `CLOUD_ENV: '请替换为你的云环境ID'` 改为「云开发 → 设置 → 环境」中看到的环境 ID（如 `'fitlog-1g2h3k4i5j6k'`）。
  2. **建集合 + 设权限**：云开发控制台 → 数据库 → 新建集合 `users / workouts / sets / plans / bodyMetrics`；每个集合权限设为「仅创建者可读写」（安全规则即 `doc._openid == auth.openid` 的读/写）。`_openid` 由云环境在写入时自动注入，无需手动维护。
- **验证期踩坑（微信开发者工具）**：用外部工具（非 IDE 编辑器）新增的 `utils/auth.js` / `cloud.js` / `config.js` 不会被 IDE 的编译期依赖图自动打包，运行时报 `module 'utils/auth.js' is not defined`，且首页状态卡片变灰/残缺（即最初看到的「灰色卡片」）。解决：**「工具 → 清缓存 → 全部清除」后重新编译**即可，无需改代码。另：`ideGetAlarmGroupMembers` 报错是 IDE 自身拉取「云监控告警组」的后台请求失败，与项目代码无关，可忽略。
- **验证期 UI 修复**：云端卡片变绿后用户反馈「点不了浏览动作库」。逐一核验路由/绑定/数据层/动作库页四文件均无问题，定位为首页 `.hero` 的 `justify-content: center` 在矮屏把末项（按钮）挤出可视区且顶部溢出不可滚动。改为 `flex-start` + `padding-bottom` 后正常跳转（提交 `236897e`）。经验：小程序首页 hero 区**避免用 `justify-content:center` 居中可能溢出的内容**，否则末项会在小屏不可点。
- **提交**：`a4c25fe` / `236897e`

### 2026-07-11 · 功能 #3 完成 — 结构化训练记录（选动作 → 逐组录入次数/重量/休息 → 写云）
- **本次范围**（对应 P0「结构化训练记录」）：
  - 新增记录页 `pages/record`（`record.wxml/.js/.wxss/.json`），`app.json` 注册路由；首页 `pages/index` 新增「开始训练」入口按钮跳转。
  - 交互：顶部显示当天日期；从内置动作库选择器（搜索 / 部位分类 chips）选动作加入「当前训练」；每个动作可逐组录入「次数 / 重量(kg) / 休息(秒)」，支持增删组、移除动作；空态提示。
  - 保存：先写 `workouts` 集合（含 date/dateStr/title/exercises 概要/setTotal/createdAt），再批量写 `sets` 集合（每组 sessionId/exerciseId/exerciseName/setIndex/reps/weight/restSec/completed）；写前 `auth.ensureUser()` 确保用户档案存在（复用免登录模式，`_openid` 自动注入）。
  - 数据校验：至少添加一个动作；至少一组填了次数或重量才允许保存；空字段存 `null`（自重 / 计时类友好）。
  - 复用：`utils/exerciseData.js` 提供动作列表与 `getById`；`utils/cloud.js` 提供集合引用；`utils/auth.js` 提供 `ensureUser`。
- **与 PRD 数据模型的差异说明**：PRD §5.2 称集合为 `training_sessions`，实际代码用 `config.COLLECTIONS.WORKOUTS = 'workouts'`（§5 字段 `userId` 在免登录模式下由云自动注入的 `_openid` 替代，不手动存）。字段命名以 `utils/config.js` 为准。
- **未做（留给后续功能）**：训练历史列表与详情（#4）、训练计划 / 模板（#5）、统计趋势 / 打卡日历 / 身体数据（#6）、计时器 / 深色模式 / 导出（#7）、保存后自动跳历史页。
- **自测结论**：node 语法检查（record.js / index.js 通过）；逻辑冒烟测试（mock 微信环境 + mock 云数据库，`scripts/test_record_save.js`）：选动作 → 填 2 组 → 保存，验证「workouts 写 1 条 + sets 写 2 条、数值类型正确、空休息存 null、exerciseName/setIndex 正确、users 档案写入不串入 sets」，结果 `✅ 自测通过`。
- **验证要点（你在微信开发者工具）**：需 `workouts` / `sets` 集合已建且权限「仅创建者可读写」（同 #2 配置）；首次进入记录页点「保存」会先触发云端登录建档；建议「清缓存 → 全部清除 → 重新编译」后再跑，避免 #2 已记录的 IDE 编译缓存坑。
- **提交**：`2e60355`

### 2026-07-12 · 功能 #4 完成 — 训练历史列表 + 训练详情
- **本次范围**（对应 P0「训练历史列表 + 训练详情（按日期倒序）」）：
  - 新增历史列表页 `pages/history`：读取 `workouts` 集合（`.limit(100)`），客户端按 `date` 倒序（`utils/historyData.js` 的 `sortWorkouts`），卡片显示日期 / 动作数·总组数 / 动作名摘要（最多 3 个 + 「等N项」）；点按 `goDetail` 带 `id` 跳详情；空态提示。
  - 新增详情页 `pages/history-detail`：并行读 `workouts.doc(id)` + `sets.where({sessionId:id})`，用 `buildGroups` 按 `workout.exercises` 顺序分组、组内按 `setIndex` 升序；渲染头部（日期 / 动作数·总组数）与逐组逐组明细。
  - 显示格式化：次数/重量/休息空值显示 `—`，`weight === 0` 显示「自重」，否则 `X kg`、`Y s`；避免 WXML 内写三元表达式。
  - 首页 `pages/index` 新增「训练历史」入口；`app.json` 注册 `pages/history`、`pages/history-detail` 路由。
  - 抽出纯函数 `utils/historyData.js`（`sortWorkouts` / `buildGroups`），不依赖 `wx`/云，便于 node 单测。
- **设计取舍（避免云控制台建索引）**：列表读取不依赖 `orderBy`（客户端排序）；详情 `sets` 查询用单字段等值 `where({sessionId})` + 客户端按 `setIndex` 排序，**不触发复合索引要求**，开箱即用无需额外控制台配置。读取走免登录模式自动按 `_openid` 过滤，无需 `ensureUser`。
- **与 PRD 数据模型的差异说明**：同 #3——集合实际名 `workouts` / `sets`（非 PRD 的 `training_sessions`），`userId` 由 `_openid` 替代。
- **未做（留给后续功能）**：训练计划 / 模板（#5）、统计趋势 / 打卡日历 / 身体数据（#6）、计时器 / 深色模式 / 导出（#7）、保存后自动跳历史页、历史删除/编辑、下拉刷新 loading 动画。
- **自测结论**：node 语法检查（historyData.js / history.js / history-detail.js / index.js 通过）；`scripts/test_history.js`：① 纯函数单测（`sortWorkouts` 倒序、`buildGroups` 分组与组内升序）；② mock 云读取验证——history 列表倒序且 `namesText` 拼接正确、history-detail 分组与 `repsText/weightText/restText` 格式化（重量 0→自重、空→—）均正确，结果 `🎉 功能 #4 自测全部通过`。
- **验证要点（你在微信开发者工具）**：`workouts` / `sets` 集合已建且权限「仅创建者可读写」（#2 配置）；历史页需先有 #3 写入的训练记录才有内容；建议「清缓存 → 全部清除 → 重新编译」后跑，避免 IDE 编译缓存坑。
- **提交**：`ebc6311`

### 2026-07-12 · 底部导航条完成 — 首页 / 动作库 / 我的
- **本次范围**：用自定义 tabBar 实现底部导航（首页 `pages/index`、动作库 `pages/exercises`、我的 `pages/mine` 三个 tab 页）。
  - 新增 `custom-tab-bar` 组件（`index.{js,wxml,wxss,json}`）：固定底栏，玻璃拟态（`backdrop-filter` 模糊 + 半透明），emoji 图标（🏠/💪/👤）+ 选中态主色 `#4f8cff` 高亮 + 图标轻微放大，底部 `env(safe-area-inset-bottom)` 安全区适配。
  - `app.json` 注册 `tabBar{custom:true}`（含三 tab 的 `pagePath`/`text`）+ 新增 `pages/mine/mine` 路由；tab 页必须 `wx.switchTab` 切换（**不能** `navigateTo`）。
  - 首页「浏览动作库」由 `navigateTo` 改为 `switchTab`；首页/动作库 `onShow` 通过 `this.getTabBar().setData({selected})` 同步高亮；「开始训练 / 训练历史」仍 `navigateTo` 到非 tab 页（记录/历史），不显示底栏（符合小程序 tabBar 行为）。
  - 新增「我的」页 `pages/mine`：云端同步状态（复用 `auth.ensureUser`）、训练次数统计（`workouts.count()`）、训练历史入口（`navigateTo`）、训练计划/数据统计「敬请期待」路线图（对应 #5/#6）。
  - 各 tab 页内容底部加安全区 padding：`.tab-page`（app.wxss，+150rpx+安全区）与 `.hero`（首页，+160rpx+安全区），避免被固定底栏遮挡。
- **设计取舍**：采用「自定义 tabBar」而非原生 tabBar —— 可读写 WXML/WXSS 完全贴合暗色玻璃主题，且无需准备 PNG 图标资源（原生 tabBar 的 `iconPath` 非必填但图标缺失观感差）。代价是每个 tab 页 `onShow` 需手动 `getTabBar().setData({selected})`，已统一处理。
- **自测结论**：node 语法检查（custom-tab-bar/index.js、mine.js、index.js、exercises.js 通过）；脚本校验 `app.json`：`tabBar.list` 三路径均已在 `pages[]` 注册、JSON 合法、`custom:true`。
- **验证要点（你在微信开发者工具）**：自定义 tabBar 属编译期特性，新增后务必「清缓存 → 全部清除 → 重新编译」；真机/模拟器底部应出现三 tab 且当前页高亮，`我的` 页显示同步状态与训练次数。
- **提交**：`e808661`

### 进度总览
| 功能 | 任务号 | 状态 | 提交 |
| --- | --- | --- | --- |
| #1 项目骨架 + 预设动作库（含 #1.5 中文译名） | #4 #5 #12 | ✅ 完成 | `da28688` / `32ba824` / 本次 |
| #2 微信登录 + 云端同步 | #6 #13 | ✅ 完成 | `a4c25fe` / `236897e` |
| #3 结构化训练记录 | #7 | ✅ 完成 | `2e60355` |
| #4 训练历史 + 详情 | #8 | ✅ 完成 | `ebc6311` |
| 底部导航条（首页/动作库/我的） | #15 | ✅ 完成 | `e808661` |
| #5 训练计划 / 模板 | #9 | ⬜ 待开始 | — |
| #6 统计 / 日历 / 身体数据 | #10 | ⬜ 待开始 | — |
| #7 打磨：计时器 / 深色 / 导出 | #11 | ⬜ 待开始 | — |
